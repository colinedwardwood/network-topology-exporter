# Build output
bin/
dist/

# Test output
coverage.out
coverage.html
*.test
*.out

# Benchmark result files (scripts/run-scale-bench.sh)
scripts/bench-results/

# Editor / OS junk
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Local env
.env
.env.local
*.env

# Secrets / credentials — never commit
**/id_ed25519
**/id_ed25519.pub
**/id_rsa
**/id_rsa.pub
**/id_ecdsa
*.pem
*.key
*.p12
*.pfx
deploy/long-running-test/*.secret*
deploy/long-running-test/.env

# Snapshot / state from local runs
snapshot.json
**/snapshot.json

# SBOM output
sbom/
*.spdx.json

# go module cache (per-machine)
vendor/

# Claude Code agent worktrees and built binaries at repo root
.claude/worktrees/
/topology-exporter
clab-nte-e2e/

# Personal scratchpad — not a project artifact
TODOs.txt

deploy/long-running-test/.last-topo

# Personal: implementation plans with concrete homelab paths
docs/superpowers/plans/

# Personal: deploy.sh is pinned to colinwood homelab; rewrite parameterized
# version per deployment before sharing.
deploy/long-running-test/deploy.sh
